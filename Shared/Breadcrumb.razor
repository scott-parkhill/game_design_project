@code
{
    [CascadingParameter(Name = "BreadcrumbContainer")] BreadcrumbContainer? Container { get; set; }
    [Parameter] public RenderFragment ChildContent { get; set; }
    [Parameter] public string? Href { get; set; }

    protected override void OnInitialized() => Container?.AddBreadcrumb(this);
}